#!groovy

properties([disableConcurrentBuilds()])

pipeline {
    agent {
        label any
    }
    options {
        buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
        timestamps()
    }
    stages {
        stage("First step") {
            steps {
                sh 'ssh root@worker1 \'hostname\''
            }
        }
    }
}
